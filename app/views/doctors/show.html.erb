<%= @doctor.name %>
Department: <%= @doctor.department %>
<%= @doctor.appointments.each do |appointment| %>
<li>
  <%= app.appointment_datetime.strftime("%B %d, %Y at %H: %M") %> - <%= link_to appointment.patient.name, patient_path(appointment.patient) %>
</li>
<%end%>
